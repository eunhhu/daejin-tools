[Unit]
Wants=warp-svc.service
After=warp-svc.service

[Service]
# Route only CourseCrawler's requests.Session through the local WARP SOCKS5 proxy.
Environment="DAEJIN_UPSTREAM_PROXY=socks5h://127.0.0.1:18088"
