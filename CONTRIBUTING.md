# 개발 가이드

## 기능 하나를 추가할 때

1. `apps/<feature>/`에 실제 실행 코드와 그 기능의 README를 추가합니다.
2. 해당 기능에 필요한 의존성만 앱 자체 manifest/requirements로 선언합니다.
3. `tests/<feature>/`에 네트워크 없이 검증 가능한 테스트부터 작성합니다.
4. 여러 앱이 실제로 쓰는 코드가 생기면 `src/daejin_tools/<module>.py`로 추출합니다.
5. Python 앱이 공통 패키지를 쓰면 루트 패키지를 설치합니다. 임의의 `sys.path` 수정은 피합니다.
6. 새 앱의 테스트 의존성이 있으면 CI의 설치 단계에도 명시합니다.

`apps/sugang`은 기존 스크립트 형태를 보존한 예외입니다. 새 앱에 import 시 로그인,
네트워크 연결, 파일 생성, 백그라운드 작업 시작 같은 부작용을 복제하지 않습니다.

## 검증

루트 README의 개발 환경을 설치한 뒤 아래 명령을 실행합니다.

```bash
python -m pytest -q -W error
python -m ruff check src tests
python -m compileall -q src apps tests
python -m build
```

수강 레거시 앱 전체를 새 린트 규칙으로 일괄 재작성하지 않습니다.
현재 린트 범위는 공통 코드와 테스트이며, 기존 앱은 구문 컴파일과 회귀 테스트로 검증합니다.

실제 계정, 학교 API, 운영 캐시, 알림 전송, 시스템 서비스를 테스트에 연결하지 않습니다.
실제 학번·비밀번호·구독 정보·키 파일은 커밋하지 않습니다.

## 릴리스

일반 CI는 운영 서비스를 배포하지 않습니다.
기존 수강 GUI는 `sugang-v*` 태그만 사용하도록 분리했고, 다른 기능의 릴리스 방식은
그 기능을 구현할 때 별도로 정합니다. 패키지 버전과 기존 GUI 릴리스 버전은 별개입니다.
